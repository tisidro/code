

let printerArray = (fruits) => {
    for (var i = 0; i < fruits.length; i++)

        return fruits;
}

console.log(printerArray(["apple", "plum", "peach"]))

/* code used to be

function printerArray(pets) {
    for (var i = 0; i < pets.length; i++)

        console.log(pets[i]);
}

printerArray(["cat", "dog", "fish"])

//cat dog fish*/