
//GO TO LAST 15 MIN OF 7/13 CLASS AND DO THIS ONE
let names = ['Alice', 'Bob', 'Charlie', 'Carol', 'David'];

let bigNames = names.map(upper);
;
let splitNames = names.map(splitter);

console.log(splitNames)

function counter(word) {
    return workerData.length;
}

let countNames = names.map(count);

console.log(countNames)